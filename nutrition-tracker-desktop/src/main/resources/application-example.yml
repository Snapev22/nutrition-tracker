spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nutrition_tracker_test
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true